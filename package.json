{
  "name": "jenkins-docker-kubernetes-nodeapp",
  "version": "1.0.0",
  "description": "This is simple Node web application used for CICD pipeline demo with Jenkins, Docker and Kubernetes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
